class Solution:
    def findPeakElement(self, nums: List[int]) -> int:
        #max elt will always be the peek
        i=0
        j=len(nums)-1
        while i<=j:
            mid=(i+j)//2
            if (mid==0 or nums[mid]>nums[mid-1]) and (mid==len(nums)-1 or nums[mid]>nums[mid+1]):
                return mid
            elif nums[mid]<nums[mid+1]:
                i=mid+1
            else:
                j=mid-1
        
    


Missed out on the edge cases check initially that caused it to fail
mid==0 and mid==len(nums)-1 are imp edge cases
