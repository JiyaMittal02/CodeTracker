class Solution:
    def divisors(self, n):
        l1=[]
        i=1
        while i<=num:
            if num%i==0:
                l1.append(i)
            i+=1
        return l1
   
    OPTIMIZED VERSION-- use sqrt like we do in prime numbers to prevent checking all  num from 1 to nth instead check half of them only
