class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:

        carry=1
         
        for i in range(len(digits)-1,-1,-1):

            result=digits[i]+carry
            if result>9:
                digits[i]=result%10
                carry=result//10
    
            else:
                digits[i]=result
                carry=0
                break
        if  carry>0:
            digits.insert(0,carry)
         
        return digits 


/// JAVA SOL
class Solution {
    public int[] plusOne(int[] digits) 
    {
        for(int i=digits.length-1;i>=0;i--)
        {
            if(digits[i]<9)
            {
                digits[i]++;
                return digits;
            }
            digits[i]=0;
        }
        int[] result=new int[digits.length+1];
        result[0]=1;
        return result;
    }
} 

O(n) , O(1)
EDGE : ALL are 9's

The brute force I thought of was to multiply by power of ten and sum it up but it is more time consuming as it involves converting the result to string and then 
to int again also handling the edge case OF WHEN ALL are 9's

            

        
